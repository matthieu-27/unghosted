name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  web:
    name: web (biome, tsc, vitest, build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.12"
      - run: bun install --frozen-lockfile
      - run: bun run check
      - run: bun run typecheck
      - run: bun run test
      - run: bun run build

  api:
    name: api (ruff, mypy, pytest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: unghosted
          POSTGRES_PASSWORD: unghosted
          POSTGRES_DB: unghosted
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      mongo:
        image: mongo:8
        env:
          MONGO_INITDB_ROOT_USERNAME: unghosted
          MONGO_INITDB_ROOT_PASSWORD: unghosted
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --quiet --eval 'db.runCommand({ping:1}).ok'"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      UNGHOSTED_DATABASE_URL: postgresql+asyncpg://unghosted:unghosted@localhost:5432/unghosted
      UNGHOSTED_MONGO_URL: mongodb://unghosted:unghosted@localhost:27017
    steps:
      - uses: actions/checkout@v4
      # setup-uv publishes immutable releases since v8: pin the exact version.
      - uses: astral-sh/setup-uv@v10.0.1
      - run: uv sync --all-groups
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run mypy src tests
      - run: uv run pytest

  audit:
    name: dependency audit (pip-audit, bun audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v10.0.1
      - name: Python dependencies
        run: |
          cd apps/api
          uv export --frozen --no-emit-project -o requirements.txt
          uvx pip-audit -r requirements.txt
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.12"
      - name: Web dependencies
        run: |
          cd apps/web
          bun install --frozen-lockfile
          bun audit

  prose:
    name: prose (vale)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Vale 3.22.0
        run: |
          curl -sL https://github.com/errata-ai/vale/releases/download/v3.22.0/vale_3.22.0_Linux_64-bit.tar.gz | tar xz vale
          sudo install -m 0755 vale /usr/local/bin/vale
      # vale only exits nonzero on error-level alerts; the project baseline
      # is zero warnings, so also fail when the "✖" summary marker appears.
      - name: Vale (fail on any alert)
        run: |
          out=$(vale . --glob='*.md' 2>&1); ec=$?
          printf '%s\n' "$out"
          [ "$ec" -ne 0 ] && exit "$ec"
          if printf '%s' "$out" | grep -q '✖'; then exit 1; fi
